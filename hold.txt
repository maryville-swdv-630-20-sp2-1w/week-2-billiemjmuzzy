     print(f"Your current balance is ${balance}")

     def get_details(self):
        """
        Method to return account information
        """
        print(
            f'''
              Account Number: {self.__account_number}

              Name:           {self.__first_name} {self.__last_name}

              Address:        {self.__street}
                              {self.__city}, {self.__state}
                              {self.__zip_code}

              Balance:        ${"{:.2f}".format(self.__balance)}

              '''
        )

try:
            amt = float(input("Please input amount to be deposited: "))
            self.__balance += amt
            new_balance = "{:.2f}".format(self.__balance)
            print(f"Your current balance is ${new_balance}")
        except:
            print("Please restart transaction and enter a number")


 try:
            amt = float(input("Please input amount to withdraw: "))
            self.__balance -= amt
            new_balance = "{:.2f}".format(self.__balance)
            print(f"Your current balance is ${new_balance}")
        except:
            print("Please restart transaction and enter a number")


def transaction(self):
        """
        processes transactions
        """
        first_name = self.__first_name
        print(f"Welcome, {first_name}, to the Bank of Billie!")
        choose = input(
            f'''
            Please select:
            [w] for withdrawl
            [d] for deposit
            [b] for current balance
            [i] for account information
            any other key to quit:

            ''')

        if choose == "w" or choose.lower() == "d" or choose.lower() == "b" or choose.lower() == "i":
            while choose == "w" or choose == "d" or choose == "b" or choose == "i":
                if choose.lower() == "w":
                    self.withdrawl()

                elif choose.lower() == "d":
                    self.deposit()

                elif choose.lower() == "b":
                    self.get_balance()

                elif choose.lower() == "i":
                    self.get_details()

                choose = input(
                    f'''
            Please select:
            [w] for withdrawl
            [d] for deposit
            [b] for current balance
            [i] for account information
            any other key to quit:

            ''')

        else:
            print("Thank you and have a great day!")
